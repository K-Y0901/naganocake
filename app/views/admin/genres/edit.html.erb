<h4>ジャンル編集</h4>
<%= form_with url: admin_genre_path, model:@genre, local: true do |f| %>
 <h5>ジャンル</h5>
  <%= f.text_field :name %>
  <label><%= f.radio_button :is_active, "true" %>有効</label>
  <label><%= f.radio_button :is_active, "false" %>無効</label>
  <%= f.submit '変更を保存する' %>
<% end %>